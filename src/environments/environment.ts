// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  backendApi: 'https://s2dev.marity.ro/api/',
  s1Api: 'https://s1dev.marity.ro/api/',
  businessWebSite: 'https://business-landing-page-dev-kcjyq723uq-ew.a.run.app',
  client360WebSite: 'https://marity-360planner-test-kcjyq723uq-ew.a.run.app',
  // backendApi: 'http://localhost:8000/api/',
  // s1Api: 'http://localhost:8080/api/',
  // backendApi: 'http://192.168.1.103:8000/api/',
  // backendApi: 'http://86.124.130.239:8000/api/'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
